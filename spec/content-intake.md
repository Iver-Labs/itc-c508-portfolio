# Content Intake Form

Fill in your real answers below (just edit this file, or reply in chat) and
they'll get swapped into `about.html`, `prelims.html`, `midterms.html`, and
`finals.html` in place of the current draft/placeholder content.

## About Me (`about.html`)

1. **Full name:** Isabela Loraine Reyes _(taken from your email — correct if wrong)_
2. **Program / year** (e.g. "3rd year BSCS"):
3. **Bio — 2–3 sentences** about who you are and what drew you to deep learning/NLP:
4. **Anything else about you** (interests outside the course, goals for this term):
5. **Skills & tools** — keep/remove/add from: Python, TensorFlow/Keras, PyTorch, NLP, Jupyter/Colab. Add any others (e.g. Pandas, scikit-learn, Git):
6. **GitHub username** (for the contact link — currently `yourusername`):
7. **Profile photo** — do you have a file to use? If yes, drop it in `images/` and tell me the filename.
8. **Snapshots section** — keep the 3 optional extra-image placeholders, or remove that section entirely?

## Prelims (`prelims.html`)

Currently drafted as generic placeholders:
- Activity 01 · Week 2 — "Perceptron & Activation Functions"
- Activity 02 · Week 5 — "Feedforward Neural Network on MNIST"

For each activity you actually did this period, give me:
1. **Week/date:**
2. **Title:**
3. **What you built / what it was** (1–2 sentences, specific: dataset, approach, result) — goes in the activity card AND the report page's Description:
4. **Tags** (languages/frameworks used):
5. **Report screenshots** — one or more filenames in `images/` (e.g. loss curve, notebook cell, output), or "none yet" — these go in that activity's report page gallery:
6. **Reflection** — what you learned, what was difficult, or what you'd do differently on this activity. This is the part I can't write for you — it goes on the report page under "Reflection."
7. **Attachment (optional)** — a notebook/doc you also want linked from the report page (Colab, GitHub, Drive, etc.), or "none":

_(List as many activities as you actually have — not limited to 2. Each
activity now has its own report page at `reports/<period>-<number>.html`,
linked from both its activity card and the Report index page.)_

## Midterms (`midterms.html`)

Currently drafted as generic placeholders:
- Activity 01 · Week 7 — "Text Preprocessing & Word Embeddings"
- Activity 02 · Week 9 — "Sentiment Classification with an LSTM"

Same 6 questions as above, per real activity.

## Finals (`finals.html`)

Currently drafted as generic placeholders:
- Activity 01 · Week 12 — "Sequence-to-Sequence Text Summarization"
- Activity 02 · Week 14 — "Fine-Tuning a Transformer for Text Classification"

Same 6 questions as above, per real activity.

## Notes

- Each activity's report page already exists as a draft (title, meta,
  screenshot placeholders, the same description as its activity card, and
  a "Reflection pending" placeholder) — answering question 6 above just
  replaces that placeholder text with your real reflection.
- If a future activity's report page doesn't exist yet, that's fine too —
  the site shows "Report pending" instead of a dead link (per `design.md`'s
  Error Handling rules), so you don't need to wait to give me everything at
  once.
- You can answer just the About Me section first and send period content
  later, or vice versa — I'll update whichever files have new answers.
